# 2.0.2

## 测试发布
- 这是用于验证桌面端自动更新链路的测试版本。
- 验证 GitHub Release assets 是否正确包含 Windows 安装包、blockmap 和 `latest.yml`。
- 验证桌面端能读取 Release Notes，并从 `2.0.1` 检测到 `2.0.2`。

## 变更
- 将应用版本提升到 `2.0.2`，用于触发 `electron-updater` 的真实更新检测。

## 注意
- 这是临时测试版本，测试完成后可以删除 GitHub Release 和 tag。
